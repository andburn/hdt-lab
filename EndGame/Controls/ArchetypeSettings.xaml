<UserControl x:Class="HDT.Plugins.EndGame.Controls.ArchetypeSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HDT.Plugins.EndGame.Controls"
             xmlns:hdt="clr-namespace:Hearthstone_Deck_Tracker.Controls;assembly=HearthstoneDeckTracker"
             xmlns:mahapps="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <ComboBox x:Name="DeckClassFilterSelection"></ComboBox>
            <ComboBox x:Name="DeckFormatFilterSelection"></ComboBox>
            <ListBox x:Name="DeckList"
                     SelectionChanged="DeckList_SelectionChanged" SelectionMode="Single"></ListBox>
            <Button Click="ButtonNew_Click">New</Button>
            <Button>Delete</Button>
        </StackPanel>
        <local:ArchetypeDeckView x:Name="ArchetypeDeck" Grid.Column="1" />
    </Grid>
</UserControl>