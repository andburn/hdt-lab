<Window x:Class="HDT.Plugins.EndGame.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:HDT.Plugins.EndGame.Windows"
        xmlns:hdt="clr-namespace:Hearthstone_Deck_Tracker.Controls;assembly=HearthstoneDeckTracker"
        Topmost="True"
        mc:Ignorable="d"
        Width="400" Height="600"
        d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListView x:Name="PlayedDeck" Grid.Column="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <hdt:Card />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ListView x:Name="ArchetypeDeck" Grid.Column="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <hdt:Card />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>