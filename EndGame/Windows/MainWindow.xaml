<Window x:Class="HDT.Plugins.EndGame.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:HDT.Plugins.EndGame.Windows"
        xmlns:hdt="clr-namespace:Hearthstone_Deck_Tracker.Controls;assembly=HearthstoneDeckTracker"
        Topmost="True"
        mc:Ignorable="d"
        Width="600" Height="600"
        d:DesignHeight="300" d:DesignWidth="300">
    <Grid ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <DockPanel>
                <ComboBox x:Name="ArchetypeSelection">
                </ComboBox>
                <Button x:Name="ArchetypeAdd" DockPanel.Dock="Right" Width="24">+</Button>
            </DockPanel>
            <ListView x:Name="PlayedDeck">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <hdt:Card />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <StackPanel Grid.Column="1">
        </StackPanel>
    </Grid>
</Window>